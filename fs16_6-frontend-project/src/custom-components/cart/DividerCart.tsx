import { Divider, styled } from '@mui/material'

const DividerCart = styled(Divider)(
    ({ theme }) => (
        {
            width: '100%',
            bgcolor: 'coral'
        }
    )
)

export default DividerCart